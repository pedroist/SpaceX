export interface Launch {
    id: string,
    img?: string,
    name?: string,
    date?: Date,
    details?: string,
    success?: boolean,
    classification?: string,
    rocketId?: string
    rocketName?: string,
}